#ifndef _3_FUNCTION_LIKE_MACRO_H_
#define _3_FUNCTION_LIKE_MACRO_H_

/*To find the absolute value of (x)*/
#define ABS(x) ((x) < 0 ? (-(x)) : (x))

/*if the input(x) is negative add a negative*/
/* otherwise, just return the number because its a positive*/

#endif
